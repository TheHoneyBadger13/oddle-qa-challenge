{
  "dependencies": {
    "cypress": "^12.7.0",
    "mocha-junit-reporter": "^2.2.0"
  },
  "scripts": {
    "cypress": "cypress run",
    "cypress:chrome": "cypress run --browser chrome --spec cypress/e2e/specs/*"
  }
}
